# Database
# ──────────────────────────────────────────────────────────────────────────────
# Use your real production DB URL here (Postgres, MySQL, etc.)
# Format: dialect+driver://username:password@host:port/database
DATABASE_URL=postgresql://myuser:mypass@db-host.example.com:5432/pollerdb

# Connection Pooling (optional—you can tweak these for your workload)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# CORS
# ──────────────────────────────────────────────────────────────────────────────
# Which front-end origins are allowed to call your API
CORS_ORIGIN=http://localhost:3000

# Logging (optional)
# ──────────────────────────────────────────────────────────────────────────────
# e.g. DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
